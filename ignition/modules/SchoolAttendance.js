

const { buildModule } = require("@nomicfoundation/hardhat-ignition/modules");

module.exports = buildModule("SchoolAttendance", (m) => {
  const SchoolAttendance = m.contract("SimplSchoolAttendance");

  return { SchoolAttendancelet };
});
